import { CurrencyUgxPipe } from './currency-ugx-pipe';

describe('CurrencyUgxPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyUgxPipe();
    expect(pipe).toBeTruthy();
  });
});
